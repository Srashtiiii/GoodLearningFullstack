let str="Hello world Learning Strings";
//to split the string into array with respect of spaces
let arr=str.split(" ");
console.log(arr);


//to join the array into a string
let newstr=arr.join("_");
console.log(newstr);

let indx=str.indexOf("world");
console.log(indx);
